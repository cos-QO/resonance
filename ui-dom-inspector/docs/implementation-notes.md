# Implementation Notes

This file exists to collect implementation notes as the `UI DOM Inspector` is built.

Suggested sections to fill in as work progresses:

- manifest decisions
- permissions model
- screenshot compression approach
- selected-element data schema
- token diagnostic heuristics
- component hint heuristics
- bridge transport choice
- MCP tool schema updates
