# Project File Tree

**Status**: Ready for analysis
**Last Updated**: Not analyzed yet
**Analyzed by**: `/prepare` command

> ⚠️ Stub file. Populated by `/prepare` in each target project — do not edit in cc-ready source.

## Auto-Analysis Process

When `/prepare` runs, it will:
1. Walk the project directory (respecting `.gitignore`)
2. Document the top-level folder layout
3. Identify key directories (source, tests, config, docs)
4. Update this file with a current tree and timestamp

## Sections `/prepare` Will Populate

### Current Project Structure
- Authoritative map of top-level directories and key files
- Purpose of each directory

### Key Project Discoveries
- Primary language, documentation approach, configuration format
- Memory/agent system status

### Development Workflow
- Entry points, build commands, test commands
