# Requirements for Claude Code Multi-Agent Development System
# These packages are needed by the hooks and scripts in .claude/

# Core Python dependencies for hook system
pathlib2>=2.3.0  # Enhanced path operations (built-in in Python 3.4+, but ensures compatibility)
json5>=0.9.0     # JSON parsing with comments support for configurations
psutil>=5.8.0    # System and process utilities for monitoring and optimization

# Web and API dependencies
requests>=2.28.0 # HTTP requests for web hooks, security research, and API calls
urllib3>=1.26.0  # URL handling and HTTP client functionality

# Configuration and data processing
pyyaml>=6.0      # YAML configuration parsing and processing
toml>=0.10.2     # TOML configuration file support

# Development and testing utilities
pytest>=7.0.0    # Testing framework for validation scripts
black>=22.0.0    # Code formatting for Python files
flake8>=4.0.0    # Code linting and quality checks

# Security and validation
cryptography>=3.4.8  # Security-related functionality
validators>=0.18.2   # Input validation utilities

# System monitoring and performance
watchdog>=2.1.0  # File system monitoring for hooks
colorama>=0.4.4  # Cross-platform colored terminal text

# Optional dependencies for enhanced features (install with: pip install -r requirements.txt[full])
# These are automatically installed by the /new command setup process

# ── Orchestrator + TUI ────────────────────────────────────────────────────────
httpx>=0.27.0        # HTTP client for Linear GraphQL API
python-dotenv>=1.0.0 # Loads .env file into environment variables
typer>=0.12.0        # CLI framework for the resonance command
rich>=13.0.0         # Terminal formatting for resonance status output
textual>=0.60.0      # TUI framework for resonance watch dashboard (Milestone 2)
pydantic>=2.0.0      # Data models and validation